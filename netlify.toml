# Netlify config for the Daily Briefing static site.
# No build step: the site is plain HTML/CSS/JS served from the repo root.

[build]
  publish = "."
  command = ""

[[headers]]
  for = "/fabian-learning.md"
  [headers.values]
    Cache-Control = "no-cache"
